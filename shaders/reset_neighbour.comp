#version 450
layout (local_size_x = 64) in;

#include comp_shader.incl

// Main
//----------------------------------------------------------------------------------------------------------------------
void main()
{
    //grab global ID
    uint gID = gl_GlobalInvocationID.x;

    if (gID < nb_nr * ubo.nr_of_particles) {
        neighbour_data.numbers[int(gID)] = 0;
    }
}